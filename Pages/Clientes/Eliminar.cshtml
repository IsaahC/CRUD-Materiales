@page
@model CRUD_Materiales.Pages.Clientes.Eliminar
@{
    ViewData["Title"] = "Eliminar";
}

<div class="mx-auto rounded border-p4" style="width:auto 400px">
    <h2 class="text-center mb-3">Eliminar cliente</h2>
    <hr />
    <p>¿Confirma Eliminar al cliente?</p>
    <p>Cliente ID=@Request.Query["id"]</p>
    <hr/ >
    <form method="post">
        <div class="row">
            <div class="col-sm-6 d-grid">
                <button type="submit" class="btn btn-danger">Eliminar</button>
            </div>
            <div class="col-sm-6 d-grid">
                <a class="btn btn-outline-danger" href="/Clientes/Index">Cancelar</a>
            </div>
        </div>

    </form>
</div>